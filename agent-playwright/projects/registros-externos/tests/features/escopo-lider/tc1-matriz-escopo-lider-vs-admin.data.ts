/**
 * Dados do TC1 (1.16) — matriz de escopo Líder vs Admin.
 *
 * Lado Admin (testável): a lista de Registros mostra registros da org inteira
 * e a tab Provedores carrega a lista completa (provedores são compartilhados,
 * não filtram por liderado). Nenhum id de seed é necessário — asserções por
 * invariante (lista não-vazia / coerente com /stats).
 */
export const tc1Data = {} as const;
