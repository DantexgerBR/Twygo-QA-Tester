# Plan — Listagem de painéis (Twygo Widgets)

> Generated by planner. Consumed by playwright-test-generator.
> Seed: tests/seed.spec.ts
> Discovery date: 2026-05-05

---

## Status do contrato

> **Histórico**: a primeira passada deste planner (2026-05-05 manhã) bateu no **bloqueio
> por contrato** da org 36908 — `/o/36908/use_modes?tab=panels-tab` renderizava o card
> `integrations-contract-block-card` dentro do tabpanel. **Em 2026-05-05 (tarde) o usuário
> habilitou o contrato/feature na org de teste**. A re-exploração confirma: o tabpanel
> Painéis agora renderiza a listagem (vazia). Os bloqueios `test.fixme` por contrato
> foram removidos. Selectors abaixo refletem o estado real observado pós-liberação.

---

## Discovered URL & Navigation

- **Login**: `/users/login` — já tratado por `tests/setup/global-setup.ts` (storageState). Specs **NÃO** logam.
- **Listagem de Painéis (URL direta com tab query param)**: `/o/{orgId}/use_modes?tab=panels-tab`
- **Listagem de Modos de uso** (tab irmã): `/o/{orgId}/use_modes` ou `/o/{orgId}/use_modes?tab=list-tab`
- **Navegação via UI**: Sidebar (seção Configurações) link **"Menu"** (`/o/{orgId}/use_modes`) → tab **"Modos de uso"** (default) → tab **"Painéis"**.
- **Breadcrumb / page title (re-confirmado 2026-05-05 pós-liberação)**: `Menu > Modos de uso` — literal, NÃO muda quando o tab Painéis é selecionado. DOM: `<div class="page-title__inner default" id="page-breadcrumb">Menu > Modos de uso</div>`. **Sem `data-test-id`**, mas tem `id="page-breadcrumb"` (estável o suficiente para usar como locator em fallback).
  - O XML do TC1 espera "Navegação > Modos de uso" — o real é "Menu > Modos de uso". Generator deve assertar o texto real (`Menu > Modos de uso`) e adicionar comentário `// REVISAR: XML diz "Navegação > Modos de uso", real é "Menu > Modos de uso"`.

### Estado atual da listagem na org 36908 (re-explorado 2026-05-05 tarde)

- **Painéis cadastrados**: **0** ("Não há dados para exibir" no tabpanel).
- **Componentes visíveis no estado vazio**: `+ Adicionar` (link), `Filtro` (botão), search input.
- **Componentes que SÓ aparecem com dados** (não confirmados live): toggle Lista/Cards, column headers, rows com switches Ativo, ícones Editar/Duplicar/Excluir, paginação.
- **Componente `list-control`** (esperado pelo XML TC1 step 4): **não é um web-component nomeado** no DOM — é apenas uma estrutura de divs com classes chakra (`chakra-input`, `chakra-button`, etc.). Generator NÃO deve assertar `<list-control>` literal — deve assertar a presença dos sub-componentes (search, add button) que indicam que a listagem renderizou.

---

## Page Objects (suggested)

### `projects/widgets/pages/PaineisListPage.ts`

```ts
import type { Locator, Page } from '@playwright/test';
import { getOrgId } from '../../../src/utils/environment.js';

export class PaineisListPage {
  constructor(private readonly page: Page) {}

  // Navegação
  async openByMenu(): Promise<void> {
    // Sidebar > Menu > tab Painéis
    await this.page.getByRole('link', { name: 'Menu', exact: true }).click();
    await this.page.getByRole('tab', { name: 'Modos de uso' }).waitFor();
    await this.page.getByRole('tab', { name: 'Painéis' }).click();
    await this.page.waitForURL(/use_modes\?tab=panels-tab/);
  }

  async goToList(): Promise<void> {
    // Atalho: rota direta
    await this.page.goto(`/o/${getOrgId()}/use_modes?tab=panels-tab`);
    await this.page.getByRole('tab', { name: 'Painéis', selected: true }).waitFor();
  }

  // Container/breadcrumb
  getBreadcrumb(): Locator {
    // REVISAR: aguardando data-test-id "page-title-breadcrumb"; fallback usa id estável "page-breadcrumb"
    return this.page.locator('#page-breadcrumb');
  }

  getModosDeUsoTab(): Locator {
    return this.page.getByRole('tab', { name: 'Modos de uso' });
  }

  getPaineisTab(): Locator {
    return this.page.getByRole('tab', { name: 'Painéis' });
  }

  // Componentes obrigatórios da listagem (TC2)
  getAddButton(): Locator {
    // REVISAR: aguardando data-test-id "paineis-list-add-button"; é um <a>, não <button>
    // href="/o/{orgId}/panels/new" — accessible name "+ Adicionar"
    return this.page.getByRole('link', { name: '+ Adicionar' });
  }

  getFilterButton(): Locator {
    // REVISAR: aguardando data-test-id "paineis-list-filter-button"; tem id="open-filter"
    return this.page.locator('#open-filter');
  }

  getSearchInput(): Locator {
    // REVISAR: aguardando data-test-id "paineis-list-search-input"
    // Placeholder real (re-explorado 2026-05-05): "Pesquise por nome ou descrição"
    return this.page.getByPlaceholder('Pesquise por nome ou descrição');
  }

  // Empty state — observável agora (org 36908 tem 0 painéis)
  getEmptyStateText(): Locator {
    return this.page.getByText('Não há dados para exibir');
  }

  // View mode toggle (TC4) — REVISAR: NÃO observável live no estado vazio.
  // Hipótese: aparece somente quando há ao menos 1 painel cadastrado.
  // Generator deve marcar test.fixme(TC4) com mensagem "requer ≥1 painel cadastrado".
  getViewModeToggle(mode: 'lista' | 'cards'): Locator {
    // REVISAR: aguardando data-test-id "paineis-list-view-toggle-{mode}"
    return this.page.getByRole('button', { name: mode === 'lista' ? /lista/i : /cards/i });
  }

  async setViewMode(mode: 'lista' | 'cards'): Promise<void> {
    await this.getViewModeToggle(mode).click();
  }

  // Colunas (TC3, TC5) — só visíveis quando há painéis cadastrados em modo Lista
  getColumnHeader(name: string): Locator {
    // REVISAR: aguardando data-test-id "paineis-list-column-{slug}"
    return this.page.getByRole('columnheader', { name });
  }

  async sortByColumn(name: string): Promise<void> {
    await this.getColumnHeader(name).click();
  }

  // Linhas / ações por linha (TC3)
  getRow(rowIdx: number): Locator {
    // REVISAR: aguardando data-test-id "paineis-list-row-{idx}" ou usar id da entidade
    return this.page.getByRole('row').nth(rowIdx + 1); // +1 pula header
  }

  getRowActiveSwitch(rowIdx: number): Locator {
    // REVISAR: aguardando data-test-id "paineis-list-row-{idx}-active-switch"
    return this.getRow(rowIdx).getByRole('switch');
  }

  getRowActionIcon(rowIdx: number, action: 'editar' | 'duplicar' | 'excluir'): Locator {
    // REVISAR: aguardando data-test-id "paineis-list-row-{idx}-action-{action}" em TODOS os ícones
    const labelMap = { editar: 'edit', duplicar: 'content_copy', excluir: 'delete' } as const;
    return this.getRow(rowIdx).locator(`text=${labelMap[action]}`);
  }

  async getRowCount(): Promise<number> {
    return this.page.getByRole('row').count();
  }

  // Paginação (TC6)
  getPagination(): Locator {
    // REVISAR: aguardando data-test-id "paineis-list-pagination"
    // Estrutura observada na aba irmã Modos de uso: chevron_left/right + select "25 por página"
    return this.page.locator('button', { hasText: 'chevron_left' }).first().locator('..');
  }

  getNextPageButton(): Locator {
    // REVISAR: aguardando data-test-id "paineis-list-pagination-next"
    return this.page.getByRole('button', { name: 'chevron_right' });
  }

  getPreviousPageButton(): Locator {
    // REVISAR: aguardando data-test-id "paineis-list-pagination-prev"
    return this.page.getByRole('button', { name: 'chevron_left' });
  }

  async goToNextPage(): Promise<void> {
    await this.getNextPageButton().click();
  }

  async goToPreviousPage(): Promise<void> {
    await this.getPreviousPageButton().click();
  }

  // Estado de bloqueio por contrato (mantido como referência para TC7 — feature flag desabilitada)
  // Observação: a UI atual (pós-liberação na org 36908) NÃO renderiza esse card.
  // Útil só se outra org/env reproduzir o estado bloqueado para comparação.
  getContractBlockCard(): Locator {
    return this.page.getByTestId('integrations-contract-block-card');
  }

  getContractBlockTitle(): Locator {
    return this.page.getByTestId('integrations-contract-block-card-title');
  }
}
```

### `projects/widgets/pages/AppMenuPage.ts` (opcional, já existe `BasePage` em `src/pages/`)

Helper para click no link "Menu" da sidebar; pode ficar como método em `BasePage` ou em uma `MenuPage` dedicada. **Não criar se não houver outro reuso óbvio** — TC1 pode chamar `getByRole('link', { name: 'Menu' })` direto via `PaineisListPage.openByMenu()`.

---

## Key data-test-ids found

Re-explorado 2026-05-05 (tarde) com o contrato Painéis liberado na org 36908.
**Resultado**: a listagem renderiza, mas continua **SEM** nenhum `data-test-id`.
Apenas o iframe global do chat tem test-id.

| data-test-id | element | observação |
|---|---|---|
| `chat-widget-iframe` | iframe (chat Sofia) | global, ignorável |

### Outros âncoras estáveis (não-test-id) observados na listagem

| Âncora | element | observação |
|---|---|---|
| `id="page-breadcrumb"` | div (texto "Menu > Modos de uso") | usável como fallback de seletor para o breadcrumb |
| `id="open-filter"` | button "Filtro" | usável como fallback para o botão Filtro |
| `href="/o/{orgId}/panels/new"` | `<a>` "+ Adicionar" | tag `<a>` (não `<button>`) — `getByRole('link', { name: '+ Adicionar' })` |
| `placeholder="Pesquise por nome ou descrição"` | input search | placeholder estável (NÃO é "Pesquise aqui" como o XML insinuava) |

---

## Missing data-test-ids (CRÍTICO — recomendar dev adicionar)

Após re-exploração com contrato liberado, **TODOS** os elementos da listagem
de painéis continuam sem `data-test-id`. Lista mantida integralmente —
sugestão de PR separado pro time de dev (segue CLAUDE.md §2.3 / §7):

### Cabeçalho da página
- `data-test-id="page-title-breadcrumb"` no `<div class="page-title__inner" id="page-breadcrumb">` (texto "Menu > Modos de uso")

### Tabs
- `data-test-id="use-modes-tablist"` no `<div role="tablist">`
- `data-test-id="use-modes-tab-modos-de-uso"` na tab "Modos de uso"
- `data-test-id="use-modes-tab-paineis"` na tab "Painéis"

### Componentes obrigatórios da listagem (TC2) — observados live no estado vazio
- `data-test-id="paineis-list-add-button"` no `<a href="/o/{orgId}/panels/new">+ Adicionar`
- `data-test-id="paineis-list-filter-button"` no botão "Filtro" (id atual `open-filter`) — **NOVO**, não mencionado no XML
- `data-test-id="paineis-list-search-input"` no input com placeholder "Pesquise por nome ou descrição"
- `data-test-id="paineis-list-empty-state"` no `<p>Não há dados para exibir</p>` — **NOVO**, útil para asserções de empty-state

### View-mode toggle (TC4) — NÃO observado live (estado vazio)
- `data-test-id="paineis-list-view-toggle-lista"` e `data-test-id="paineis-list-view-toggle-cards"` — **a confirmar quando ≥1 painel for cadastrado**

### Tabela (TC3, TC5) — NÃO observado live (estado vazio)
- `data-test-id="paineis-list-column-nome"`
- `data-test-id="paineis-list-column-descricao"`
- `data-test-id="paineis-list-column-provedora"`
- `data-test-id="paineis-list-column-data-criacao"`
- `data-test-id="paineis-list-column-ativo"`
- `data-test-id="paineis-list-column-acoes"`

### Linhas e ações (TC3) — NÃO observado live (estado vazio)
- `data-test-id="paineis-list-row-{idx-or-id}"` em cada `<tr>`
- `data-test-id="paineis-list-row-{idx}-active-switch"` no switch da coluna Ativo
- `data-test-id="paineis-list-row-{idx}-action-editar"` (ícone `edit`)
- `data-test-id="paineis-list-row-{idx}-action-duplicar"` (ícone `content_copy`)
- `data-test-id="paineis-list-row-{idx}-action-excluir"` (ícone `delete`)

### Paginação (TC6) — NÃO observado live (estado vazio + < 25 itens)
- `data-test-id="paineis-list-pagination"` no container
- `data-test-id="paineis-list-pagination-first"` (chevron duplo esquerda)
- `data-test-id="paineis-list-pagination-prev"` (chevron esquerda)
- `data-test-id="paineis-list-pagination-next"` (chevron direita)
- `data-test-id="paineis-list-pagination-page-{n}"` em cada botão numérico
- `data-test-id="paineis-list-pagination-page-size-select"` no select "25 por página"

> **Política CLAUDE.md §2.3 / §7**: NÃO inventar CSS frágil "para fazer passar". Os fallbacks `getByRole`/`getByPlaceholder`/`#id` no Page Object acima são aceitáveis temporariamente, mas o spec deve ter `// REVISAR: aguardando data-test-id de dev` em cada uso.

---

## Sync/blocking conditions observed

1. **NPS Sofia modal** (`role="dialog"` "Em uma escala de 1 a 10..."). Aparece pós-login na org 36908. Botão "Pergunte depois" dispensa. Já é tratado por `src/utils/modals.ts::dismissCommonModals` (verificar). Re-explorando 2026-05-05 tarde, o modal NÃO apareceu (storageState já estava aceito).
2. **Consentimento necessário** (LGPD modal — "Política de privacidade", "Termos de uso", "Política de dados"). Aparece em sessão nova. Botão "Aceitar". Provavelmente também tratado pelo `dismissCommonModals` ou ficará coberto pelo storageState após primeira aceitação no globalSetup. Idem — não apareceu na re-exploração.
3. **Chat widget iframe** (`data-test-id="chat-widget-iframe"`, ref Sofia, `role="region"` "Widget de chat") — ignorável, está em iframe separado.
4. ~~**Card de bloqueio por contrato**~~ — **RESOLVIDO 2026-05-05**: contrato Painéis liberado na org 36908. O tabpanel agora renderiza a listagem (estado vazio: "Não há dados para exibir"). Selectors para o card de bloqueio mantidos no Page Object apenas como referência (TC7 — bloqueio por flag, ainda não-reproduzível nesta org).

---

## Observações sobre o componente (re-explorado 2026-05-05 tarde)

- A aba **Painéis** renderiza um componente DIFERENTE da aba irmã **Modos de uso**. Não há um web-component `<list-control>` literal — é apenas um stack de divs com classes Chakra UI (`chakra-link chakra-button`, `chakra-input`, etc.) renderizado pelo React. Generator NÃO deve assertar `<list-control>` literal; deve assertar a presença dos sub-componentes (search input + add link + filtro) como prova de que a listagem renderizou.
- **Estado atual da listagem (org 36908, 0 painéis)**:
  - `<a>` "+ Adicionar" (`chakra-link chakra-button`) com href `/o/36908/panels/new`.
  - `<button id="open-filter">` "Filtro" (com ícone `filter_alt`) — **NOVO componente não mencionado no XML**.
  - `<input placeholder="Pesquise por nome ou descrição">` (search).
  - `<p>Não há dados para exibir</p>` (empty state).
  - **Sem** toggle Lista/Cards renderizado.
  - **Sem** tabela / column headers.
  - **Sem** paginação renderizada.
- **Comparativo com aba "Modos de uso" (mesma `/use_modes`, tab irmã)**: usa um componente legado de cards (`add` button apenas com ícone, "Pesquise aqui", cards numerados, edit/delete por card). NÃO compartilha estrutura com a aba Painéis — não pode ser usada como proxy para inferir o layout de Painéis quando populado.
- **Hipótese**: os sub-componentes esperados pelo XML (view-toggle Lista/Cards, colunas Nome/Descrição/Provedora/Data de criação/Ativo/Ações, switches por linha, ícones edit/duplicate/delete, paginação) são **conditionally-rendered** baseado em `painéis.length > 0`. Confirmar criando ≥1 painel manualmente OU via API (fora do escopo deste planner — fica para quando a suíte de criação for gerada).
- **Para o generator**: TC2 pode validar só os 3 elementos confirmados (add, filtro, search) + empty state. TC3, TC4, TC5, TC6 ficam `test.fixme` aguardando seed de dados.

---

## Anti-patterns a respeitar (CLAUDE.md §7.6)

Generator DEVE seguir:

- **A.** NÃO logar no spec — `globalSetup` cuida via `outputs/.auth/storage.json`.
- **B.** NÃO hardcodar URL/orgId/credenciais — importar `getBaseUrl()`, `getOrgId()` de `src/utils/environment.ts`.
- **C.** NÃO inline helpers de UI no spec — todo seletor/ação fica em `PaineisListPage`.
- **D.** Comentários só justificam WHY (invariantes não-óbvias) — Allure step narra o WHAT.

Imports obrigatórios:
```ts
import type { Locator } from '@playwright/test';
import * as allure from 'allure-js-commons';
```

---

## Scenarios

### 1. Acessar a listagem de Painéis a partir do 'Menu'
- **Importance:** critical
- **File suggestion:** `projects/widgets/tests/features/listagem-de-paineis/acessar-listagem-paineis-menu.spec.ts`
- **Allure:** `epic('Twygo - Widgets')`, `feature('Listagem de painéis')`, `story('Acessar a listagem de Painéis a partir do Menu')`, `severity('critical')`
- **STATUS:** **READY** — feature liberada na org 36908. Sem `test.fixme`.
- **Translated steps:**
  1. `await allure.step("1. Clicar no link 'Menu' da sidebar lateral", async () => { await page.getByRole('link', { name: 'Menu', exact: true }).click(); await page.waitForURL(/\/use_modes/); })`.
  2. `await allure.step("2. Verificar que a tab 'Modos de uso' está selecionada e o breadcrumb", async () => { await expect(paineis.getModosDeUsoTab()).toHaveAttribute('aria-selected', 'true'); await expect(paineis.getBreadcrumb()).toHaveText('Menu > Modos de uso'); })` — `// REVISAR: XML diz "Navegação > Modos de uso", real é "Menu > Modos de uso"`.
  3. `await allure.step("3. Clicar na tab 'Painéis'", async () => { await paineis.getPaineisTab().click(); await page.waitForURL(/tab=panels-tab/); })`.
  4. `await allure.step("4. Verificar que a listagem renderiza", async () => { await expect(paineis.getPaineisTab()).toHaveAttribute('aria-selected', 'true'); await expect(paineis.getAddButton()).toBeVisible(); await expect(paineis.getSearchInput()).toBeVisible(); })` — `// REVISAR: XML diz "renderiza como list-control"; o DOM real não tem web-component nomeado, validamos via sub-componentes (add + search)`.

### 2. Validar componentes obrigatórios da listagem de painéis
- **Importance:** critical
- **File suggestion:** `projects/widgets/tests/features/listagem-de-paineis/validar-componentes-obrigatorios.spec.ts`
- **Allure:** epic/feature iguais; story `'Validar componentes obrigatórios da listagem de painéis'`; severity `critical`.
- **STATUS:** **PARTIAL READY** — Add/Filtro/Search são confirmáveis no estado vazio. View-toggle Lista/Cards NÃO renderiza sem dados (hipótese: aparece só com ≥1 painel). Recomendação: dividir o test em duas asserções — no estado atual vazio asserta apenas (add, filtro, search, empty state); o assert do view-toggle vai num bloco com `test.fixme(true, 'requer ≥1 painel cadastrado para renderizar view-toggle')`.
- **Translated steps:**
  1. `paineis.goToList()` — pré-condição (rota direta).
  2. `await expect(paineis.getAddButton()).toBeVisible()` — `// REVISAR: aguardando data-test-id "paineis-list-add-button"; é <a>, não <button>`.
  3. `await expect(paineis.getFilterButton()).toBeVisible()` — `// REVISAR: aguardando data-test-id "paineis-list-filter-button"; usando #open-filter como fallback`. **NOVO** — Filtro não estava no XML mas existe na UI.
  4. `await expect(paineis.getSearchInput()).toBeVisible()` — `// REVISAR: aguardando data-test-id "paineis-list-search-input"`.
  5. `await expect(paineis.getEmptyStateText()).toBeVisible()` — válido enquanto a org tem 0 painéis; remover quando seedar dados.
  6. **(test.fixme)** `await expect(paineis.getViewModeToggle('lista')).toBeVisible(); await expect(paineis.getViewModeToggle('cards')).toBeVisible();` — `// REVISAR: requer ≥1 painel cadastrado; toggle não renderiza no estado vazio`.

### 3. Validar colunas exibidas na visualização em lista
- **Importance:** critical
- **File suggestion:** `projects/widgets/tests/features/listagem-de-paineis/validar-colunas-lista.spec.ts`
- **Pré-condições:** ≥1 painel cadastrado na org.
- **STATUS:** **BLOCKED-BY-SEED** — `test.fixme(true, 'requer ≥1 painel cadastrado na org de teste')`. Org 36908 está com 0 painéis ("Não há dados para exibir"). Quando a suíte de criação for gerada, este TC volta a ser runnable.
- **Translated steps:**
  1. `paineis.goToList(); await paineis.setViewMode('lista');`.
  2. Para cada coluna `['Nome', 'Descrição', 'Provedora', 'Data de criação', 'Ativo', 'Ações']`: `await expect(paineis.getColumnHeader(col)).toBeVisible();` — `// REVISAR: aguardando data-test-id "paineis-list-column-{slug}"`.
  3. `await expect(paineis.getRowActiveSwitch(0)).toBeVisible();` (coluna Ativo = switch por linha).
  4. Para cada ação `['editar', 'duplicar', 'excluir']`: `await expect(paineis.getRowActionIcon(0, action)).toBeVisible();` — `// REVISAR: aguardando data-test-id em ícones edit/content_copy/delete`.

### 4. Alternar entre visualização em lista e cards
- **Importance:** normal
- **File suggestion:** `projects/widgets/tests/features/listagem-de-paineis/alternar-lista-cards.spec.ts`
- **STATUS:** **BLOCKED-BY-SEED** — `test.fixme(true, 'requer ≥1 painel cadastrado; o toggle Lista/Cards não renderiza no estado vazio')`. Re-explorado 2026-05-05: tabpanel Painéis com 0 painéis NÃO mostra view-toggle. Hipótese a confirmar com seed.
- **Translated steps:**
  1. `paineis.goToList()` — verificar modo Lista default (assertion via presença de `getColumnHeader('Nome')`).
  2. `await paineis.setViewMode('cards')` — verificar mudança visual (ex.: column headers somem, cards aparecem) — `// REVISAR: estrutura visual a confirmar quando seed permitir`.
  3. `await paineis.setViewMode('lista')` — verificar volta à tabela.

### 5. Ordenar listagem por cada coluna
- **Importance:** normal
- **File suggestion:** `projects/widgets/tests/features/listagem-de-paineis/ordenar-colunas.spec.ts`
- **Pré-condições:** ≥3 painéis cadastrados (com Nome/Descrição/Provedora distintos para validar ordenação).
- **STATUS:** **BLOCKED-BY-SEED** — `test.fixme(true, 'requer ≥3 painéis com nomes/descrições/provedoras distintos para validar ordenação')`.
- **Translated steps:**
  1. `paineis.goToList(); await paineis.setViewMode('lista');` — capturar ordem default (`getRowsTextByColumn`).
  2-3. `await paineis.sortByColumn('Nome')` × 2 — asc → verificar ordem alfabética crescente; desc → decrescente.
  4-5. Idem `Descrição`.
  6-7. Idem `Provedora`.
  8-9. Idem `Data de criação` (comparação cronológica).
  10-11. Idem `Ativo` (booleano: true antes de false em asc, ou inverso).

### 6. Paginação da listagem com volume de painéis
- **Importance:** normal
- **STATUS:** **BLOCKED-BY-SEED** — `test.fixme(true, 'requer >25 painéis cadastrados na org de teste para que a paginação se torne interativa')`. Org 36908 tem 0 painéis. Mesmo com 1–25 painéis, a hipótese é que paginação renderiza mas com chevrons disabled (igual à aba irmã Modos de uso, que tem 2 itens e ambos chevrons disabled).
- **Translated steps:**
  1. `paineis.goToList()` — verificar pagination visível e botão "1" ativo.
  2. `await paineis.goToNextPage()` — verificar mudança de página.
  3. `await paineis.goToPreviousPage()` — verificar volta.

### 7. Acessar listagem com a feature flag desabilitada
- **Importance:** critical
- **STATUS:** **BLOCKED-BY-ENV** — `test.fixme(true, 'requer env/org com feature flag "habilitar_paineis_do_usuario" desabilitada')`. O ambiente atual tem a flag/contrato HABILITADO. Para reproduzir o cenário de flag-off, precisamos de outra org/env (atualmente sem candidato declarado em `config/environment.json`).
- **Translated steps:**
  1. Acessar `/o/{orgId}/use_modes` — verificar tabs.
  2. `await expect(paineis.getPaineisTab()).not.toBeVisible()` — tab Painéis não deve aparecer com flag off.
  3. `await page.goto('/o/{orgId}/use_modes?tab=panels-tab')` — verificar mensagem "página não existe" (selector a confirmar quando env disponível).

### 8. Validar acessibilidade por teclado (TAB / setas / ENTER / ESC)
- **Importance:** minor
- **File suggestion:** `projects/widgets/tests/features/listagem-de-paineis/acessibilidade-teclado.spec.ts`
- **STATUS:** **READY-WITH-CAVEAT** — feature liberada; pode rodar parcialmente (Tab através de add/filtro/search). Os passos que dependem de "primeira linha" precisam de seed (≥1 painel). **Caveat documentado**: testes de teclado são frágeis — usar `page.keyboard.press('Tab')` + `expect(page.locator(':focus')).toMatchAriaSnapshot(...)` ao invés de comparar referências DOM.
- **Translated steps:**
  1. `paineis.goToList();`.
  2. `await page.keyboard.press('Tab')` repetidas vezes — após cada Tab, capturar `:focus` e validar progressão: "+ Adicionar" → "Filtro" → search input → (se houver dados) view controls (lista/cards) → primeira linha.
  3. Focar "+ Adicionar" e `await page.keyboard.press('Enter')` — verificar redirect para `/o/{orgId}/panels/new`.
  4. `await page.keyboard.press('Escape')` — verificar volta à listagem (ou abertura de modal de confirmação se houver alterações pendentes — XML é ambíguo).

---

## Resumo de status por testcase

| TC | Status | Bloqueio (se houver) |
|---|---|---|
| 1 | **READY** | — runnable hoje |
| 2 | **PARTIAL READY** | 5 dos 6 asserts rodam (add/filtro/search/empty-state/breadcrumb); o assert do view-toggle vai com `test.fixme(true, 'requer ≥1 painel')` |
| 3 | **READY** (2026-05-06) | seed de 30 painéis ativo; spec assume default Lista no viewport 1920x1080 e valida 5 colunas (Nome, Descrição, Data de criação, Ativo, Ações) — Provedora removida (não existe na UI) |
| 4 | **READY** (2026-05-06) | toggle Lista/Cards confirmado live (`#grid-view-icon`/`#list-icon`); spec roda em 1920x1080 |
| 5 | **READY** (2026-05-06) | reduzido de 5 para 3 colunas ordenáveis (Nome, Data de criação, Ativo); 7 steps no total |
| 6 | **BLOCKED-BY-SEED** | requer >25 painéis |
| 7 | **BLOCKED-BY-ENV** | requer env/org com feature flag `habilitar_paineis_do_usuario` desabilitada (não disponível em `config/environment.json` hoje) |
| 8 | **READY-WITH-CAVEAT** | runnable parcialmente; passos de "primeira linha" dependem de seed; testes de teclado são frágeis por natureza |

> **Próximos passos recomendados**:
> 1. **Gerar TC1, TC2 (parcial), TC8 (parcial)** — runnable hoje contra org 36908.
> 2. **Seedar 25–30 painéis na org 36908** (com Nome/Descrição/Provedora variados) → desbloqueia TC3, TC4, TC5, TC6.
> 3. **Identificar org com flag `habilitar_paineis_do_usuario` OFF** → desbloqueia TC7. Alternativa: pedir ao dev confirmar se "tab Painéis some quando flag está off" e validar via mock.
> 4. **PR de dev**: adicionar os `data-test-id` listados em "Missing data-test-ids" antes de a suíte ser estabilizada para CI regressivo. Sem isso, fallbacks `getByRole`/`getByPlaceholder`/`#id` carregam risco de quebrar a cada mudança de label/markup.
