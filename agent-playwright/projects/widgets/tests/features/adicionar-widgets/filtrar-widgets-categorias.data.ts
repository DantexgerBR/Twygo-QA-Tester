export const filtrarCategoriasData = {
  panelName: `Painel Filtro Categorias ${Date.now()}`,
  expectedWidgetIds: ['activity_summary', 'in_progress_contents', 'ranking', 'my_certificates'] as const,
} as const;
